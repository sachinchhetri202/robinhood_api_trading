"""Top-level package for the Robinhood crypto trading CLI.

Having an explicit __init__ here ensures that the local `src` package
is used (including subpackages like `src.utils`) even if a different
`src` package is installed in the environment.
"""

